{
  "name": "ast-ncc",
  "version": "1.0.0",
  "description": "service cell floe",
  "main": "cypress.config.js",
  "scripts": {
    "ide": "npx cypress open",
    "test": "cypress run --headed --browser chrome && npm run merge:reports && npm run create:html:report",
    "merge:reports": "mochawesome-merge cypress/reports/*.json > report.json",
    "create:html:report": "npm run merge:reports && marge --reportDir consolidate_report report.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.4.1",
    "cypress-localstorage-commands": "^2.2.1",
    "cypress-parallel": "^0.15.0",
    "cypress-file-upload": "^5.0.8",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-multi-reporters": "^1.2.4",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "dependencies": {
    "localStorage": "^1.0.4"
  }
}
